// Generated by CoffeeScript 1.6.2
(function() {
  var Model, bindable, toarray, type,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  bindable = require("bindable");

  type = require("type-component");

  toarray = require("toarray");

  Model = (function(_super) {
    __extends(Model, _super);

    /*
    */


    Model.prototype.__isModel = true;

    /*
    */


    function Model(schema, data) {
      this.schema = schema;
      if (data == null) {
        data = {};
      }
      Model.__super__.constructor.call(this, data);
    }

    /*
    */


    Model.prototype.reset = function(data) {
      this.set(data);
      return this.changed = [];
    };

    /*
    */


    Model.prototype._watching = function(property) {
      return this.schema.fetchField(this, property);
    };

    /*
     fills the model in with *all* virtuals
    */


    Model.prototype.fetchAll = function(next) {
      return this.schema.fetchAll(this, next);
    };

    /*
    */


    Model.prototype.fetchField = function(name, next) {
      return this.schema.fetchField(this, name, next);
    };

    /*
    */


    Model.prototype.fetch = function(next) {
      return this.schema.fetch(this, next);
    };

    /*
     returns TRUE if the _id is not present
    */


    Model.prototype.isNew = function() {
      return !this.get("_id");
    };

    /*
    */


    Model.prototype.validate = function(next) {
      return this.schema.validate(this);
    };

    /*
    */


    Model.prototype.save = function(next) {
      return this.schema.save(this);
    };

    return Model;

  })(bindable.Object);

  module.exports = Model;

}).call(this);
