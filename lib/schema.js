// Generated by CoffeeScript 1.6.2
(function() {
  var ChangeWatcher, Field, MemoDictionary, Model, Schema, dref, payload, type, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Model = require("./model");

  type = require("type-component");

  Field = require("./field");

  MemoDictionary = require("./memoize/dictionary");

  payload = require("./payload");

  ChangeWatcher = require("./changeWatcher");

  dref = require("dref");

  Schema = (function(_super) {
    __extends(Schema, _super);

    function Schema() {
      _ref = Schema.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    /*
    */


    Schema.prototype.isNew = function(model) {
      return !model.get("_id");
    };

    /*
    */


    Schema.prototype.model = function(data) {
      var key, model;

      if (data == null) {
        data = {};
      }
      if (type(data) === "string") {
        data = {
          _id: data
        };
      }
      model = new Model(this, {
        _id: data._id
      });
      model._memoizer = new MemoDictionary();
      model._changeWatcher = new ChangeWatcher(model);
      this.reset(model, data);
      for (key in this._methods) {
        model[key] = this._methods[key];
      }
      return model;
    };

    /*
    */


    Schema.prototype.save = function(model, next) {
      var p;

      p = payload.model(model);
      if (model.isNew()) {
        p.method("post").body(this.toObject(model, model.data, this.getFields(model._changeWatcher.flushChangedKeys(), true)));
      } else {
        p.method("put").body(this.toObject(model, model.data));
      }
      return this.fetch(p.options, next);
    };

    return Schema;

  })(Field);

  module.exports = function(options, name, linen) {
    if (options == null) {
      options = {};
    }
    if (name == null) {
      name = void 0;
    }
    if (linen == null) {
      linen = void 0;
    }
    return new Schema(Field.parseOptions(options, name, linen)).init();
  };

}).call(this);
